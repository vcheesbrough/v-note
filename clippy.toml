# Threshold for `clippy::too_many_lines` (enabled workspace-wide in `Cargo.toml`).
# Set just above the longest function at the time the lint was introduced; lower
# it as functions are split, never raise it.
too-many-lines-threshold = 300
