{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://v-note/schemas/stroke-point.schema.json",
  "title": "StrokePoint",
  "type": "object",
  "additionalProperties": false,
  "required": ["x", "y", "t"],
  "properties": {
    "x": { "type": "number" },
    "y": { "type": "number" },
    "t": {
      "type": "integer",
      "description": "Milliseconds relative to the start of the stroke."
    },
    "pressure": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Normalised stylus pressure. Present only on pressure-sensitive (solid_round style_version 2) strokes; omitted on version 1. A version-2 point without it renders at full width."
    }
  }
}
